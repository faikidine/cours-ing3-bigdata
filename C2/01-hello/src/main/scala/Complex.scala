// Ex3 — classe façon record (params du constructeur entre parenthèses).
// Ex4 — override toString (mot-clé `override` devant le `def`, pas d'@).

class Complex(real: Double, imaginary: Double) {
  // TODO : getteurs re / im — d'abord avec (), puis sans ()
  // TODO Ex4 : override def toString
}

object ComplexNumber {
  def main(args: Array[String]): Unit = {
    // TODO : new Complex(1.2, 3.4) et afficher la partie imaginaire
    // TODO : ensuite lire réel / imaginaire depuis args
  }
}
