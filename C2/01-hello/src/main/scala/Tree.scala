// Ex5 — ADT : nœud = Sum, feuilles = Var / Const.
// Corps des case class : PDF (et CM2 case class = record Java 16).

abstract class Tree

object Tree {
  // TODO : case class Sum(left: Tree, right: Tree) extends Tree
  // TODO : case class Var(n: String) extends Tree
  // TODO : case class Const(v: Int) extends Tree
}
